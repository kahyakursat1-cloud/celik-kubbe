# Çelik Kubbe — Geliştirme ve test bağımlılıkları
# Çalışma zamanı: önce `pip install -r requirements.txt`

-r requirements.txt

# Test
pytest>=7.4
pytest-qt>=4.4       # Opsiyonel — pytest-qt tabanlı GUI testleri için
# unittest stdlib zaten yeterli; tests/ pytest ile de çalışır
